@{
    var currentUser = ViewBag.CurrenstUserDetails;
}

<nav class="navbar navbar-expand-lg">
    <ul class="nav navbar-nav">
        <li><a class="btn btn-dark" asp-action="ChangePassword" asp-controller="Account"><i class="bi bi-key"></i> Change Password</a></li>
    </ul>
</nav>

@*<p class="display-5 mt-5">@currentUser.FirstName @currentUser.LastName</p>
    <div class="container">
    <div class="row">
    <div class="col-sm">
    </div>
    <div class="col-sm">
    <p>First Name: <a>@currentUser.FirstName</a></p>
    <p>Last Name: <a>@currentUser.LastName</a></p>
    <p>Email: <a>@currentUser.Email</a></p>
    <p>User account created on <a>@currentUser.UserCreationDate</a> at <a>@currentUser.UserCreationTime</a></p>
    </div>
    <div class="col-sm">
    </div>
    </div>
    </div>*@



<div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
        <div class="col-md-12 col-xl-4">

            <div class="card" style="border-radius: 15px;">
                <div class="card-body text-center">
                    <div class="mt-3 mb-4">
                        <img src="~/images/avatar.png"
                             class="rounded-circle img-fluid" style="width: 100px;" />
                    </div>
                    <h4 class="mb-2">@currentUser.FirstName @currentUser.LastName</h4>
                    <p class="text-muted mb-4">
                        Joined on @currentUser.UserCreationDate<span class="mx-2">|</span> <a>@currentUser.Email
                    </p>
                    @*<div class="px-3">
                        <p class="mb-2 h5">3</p>
                        <p class="text-muted mb-0">Enrolled courses</p>
                    </div>*@
                </div>
            </div>

        </div>
    </div>
</div>